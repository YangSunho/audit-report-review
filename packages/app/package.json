{
  "name": "@ari/app",
  "version": "0.0.0",
  "type": "module",
  "description": "ARI desktop app — local web UI. Drop a .dsd, get the review. Zero external deps.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "lint": "eslint src",
    "build:standalone": "node build-standalone.mjs"
  },
  "dependencies": {
    "@ari/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "esbuild": "^0.21.5",
    "typescript": "^5.5.0"
  }
}
