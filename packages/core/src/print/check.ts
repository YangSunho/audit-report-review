// REMOVED — Print Intelligence (A4 layout checks) was dropped from the product:
// print layout is not an audit judgment and only added noise to the report.
// This file is intentionally empty; delete the `print/` directory when possible.
export {};
