{
  "name": "ari",
  "private": true,
  "version": "0.0.0",
  "description": "ARI — Audit Review Intelligence. AI-based audit review platform for DSD reports.",
  "packageManager": "pnpm@9.0.0",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "cli": "pnpm --filter @ari/cli start --"
  }
}
