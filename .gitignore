node_modules/
.pnpm-store/
dist/
build/
*.tsbuildinfo
_site/

# 빌드 산출물 — GitHub Actions 가 매번 새로 만든다.
감사보고서_검토.html

# ── 기밀 (절대 커밋 금지) ─────────────────────────────────────────────
# DSD 는 실제 감사 대상 회사의 보고서다. 공개 저장소에 들어가면 안 된다.
*.dsd
fixtures/
**/__snapshots__/
*.snap
